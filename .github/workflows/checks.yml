name: Checks
on: [push]

jobs:
  test-lint:
    name: Flake8 Test
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Lint
        uses: python3 -m flake8
      - name: Test
        uses: python3 manage.py test
